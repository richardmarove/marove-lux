# Suggested commands
- `npm install` to install dependencies.
- `npm run start` or `npx expo start` to run the app.
- `npm run android`, `npm run ios`, `npm run web` for platform-specific Expo start modes.
- `npm run lint` to run Expo lint.
- `rg --files` and `rg <pattern>` are preferred for fast repo search.

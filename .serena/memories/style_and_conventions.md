# Style and conventions
- TypeScript with exported interfaces in `types/index.ts`.
- React function components with hooks (`useState`, `useEffect`, context hooks).
- Centralized persistence/state logic in `context/DataContext.tsx`.
- Styling uses `StyleSheet.create` and shared design tokens from `theme/theme.ts`.
- Naming is straightforward and descriptive; route files follow Expo Router conventions.
